# Switching dimensions cancels the anvil interaction event
execute in slime:admin/test_world run tp 0 64 0



# Assumes that the function preserves the players original dimension and position even though it was changed
tp @s ~ ~ ~ ~ ~
